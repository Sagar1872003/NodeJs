<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Movies</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .movie-card {
            position: relative;
            overflow: hidden;
        }

        .movie-card img {
            width: 100%;
            height: auto;
            display: block;
        }

        .movie-details {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.8);
            color: white;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            opacity: 0;
            transition: opacity 0.3s ease;
        }

        .movie-card:hover .movie-details {
            opacity: 1;
        }

        .movie-details h5 {
            margin: 0;
            font-size: 1.25rem;
        }

        .movie-details p {
            margin: 5px 0;
            font-size: 0.9rem;
        }
    </style>
</head>

<body class="bg-light">
    <div class="container py-5">
        <h1 class="text-center mb-4">Movies List</h1>

        <% if (movie && movie.length > 0) { %>
            <div class="row row-cols-1 row-cols-md-3 g-4">
                <% movie.map((val) => { %>
                    <div class="col">
                        <div class="movie-card rounded-2"  >
                            <img src="<%= val.movieimage %>" alt="<%= val.name %>">
                            <div class="movie-details">
                                <h5><%= val.moviename %></h5>
                                <p><strong>Description:</strong> <%= val.moviedescription %></p>
                                <p><strong>Rating:</strong> <%= val.movierating %>/5</p>
                                <p><strong>Price:</strong> $<%= val.movieprice %></p>
                                <div class="d-flex gap-2 mt-3">
                                    <a href="/deletemovie?id=<%= val.id %>" class="btn btn-danger btn-sm">Delete</a>
                                    <a href="/updatemovie?id=<%= val.id %>" class="btn btn-primary btn-sm">Edit</a>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }) %>
            </div>
        <% } else { %>
            <div class="text-center">
                <p class="text-muted">No movies available. Add one to get started!</p>
            </div>
        <% } %>

        <div class="text-center mt-4">
            <a href="/add" class="btn btn-success">Add Movie</a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>